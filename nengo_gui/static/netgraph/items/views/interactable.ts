export class InteractableView {
    constructor() {
        // make the g container
        // draw the label
        // draw the shape
        // draw the area

        // make it interactable

        // make a new class to make it resizeable
    }
}